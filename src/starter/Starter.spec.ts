describe('Starter', () => {
  it('should call all relevant methods to start the framework', () => {
    // given
    // when
    // then
  })
  it('should store the modules which it needs to use', () => {
    // given
    // when
    // then
  })
})
